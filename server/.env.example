# Copy this to server/.env and fill values for production
# PAYMOB keys (only if you will use Paymob; otherwise leave blank)
PAYMOB_API_KEY=
PAYMOB_INTEGRATION_ID=
PAYMOB_IFRAME_ID=
# If set to 'true' the /api/checkout will proxy to /api/paymob/checkout
USE_PAYMOB=false
# Server port
PORT=4000
